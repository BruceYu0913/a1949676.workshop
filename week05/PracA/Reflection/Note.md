Task 1 

In this task, I used a string array and a foreach loop to print the names and compare their Length values. Bruce was the longest name, while Ayo was the shortest. I used Array.Sort() to sort the original array alphabetically. Array.Sort() modifies the array directly and uses an introspective sorting algorithm. Because my array is small, insertion sort is used. I then used Array.Reverse() to reverse the sorted array in place.